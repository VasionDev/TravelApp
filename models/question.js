class Question {
    constructor(id, catIds, title, imageUrl, location) {
        this.id = id
        this.catIds = catIds
        this.title = title
        this.imageUrl = imageUrl
        this.location = location
    }
}

export default Question